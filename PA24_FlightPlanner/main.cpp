// main.cpp

// read waypoints file

// write flight plan file

// quit

#include <list>
#include <iostream>
#include <stdlib.h>
#include <iterator>

int main() {

	/*std::list<int> L;
	L.push_back(0);
	L.push_front(1);
	L.insert(L.end(), 2);
	copy(L.begin(), L.end(), std::ostream_iterator<int>(std::cout, " "));
	// The values that are printed are 1 2 0

	_sleep(10000);*/
	return 0;
}