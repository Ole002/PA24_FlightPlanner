#pragma once
class CfpReader {
	/*
	ToDo:
		- finish
	*/
	/*
	About CfpReader
		temporary class; reads user's flight plan.
		User's flight plan contains some route+weather info, from which
		objects (CAircraft, CRoute, ...) are generated. These objects are
		used for calculating the actual ofp.
	*/

};